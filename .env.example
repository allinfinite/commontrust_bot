# Credit Bot Configuration
CREDIT_TELEGRAM_BOT_TOKEN=your_credit_bot_token_here

# CommonTrust API (ledger) auth (required for Mutual Credit bot and API)
# Generate a long random string.
COMMONTRUST_API_TOKEN=change_me
# Where the Mutual Credit bot should reach the API (defaults to http://localhost:8000)
# COMMONTRUST_API_BASE_URL=http://localhost:8000

# Optional: run the API in hub mode to proxy per-group ledgers (federation routing)
# LEDGER_MODE=hub
# Required in hub mode (Fernet key). Generate with: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# HUB_REMOTE_TOKEN_ENCRYPTION_KEY=

# PocketBase Configuration
POCKETBASE_URL=http://localhost:8090
# Preferred (avoid storing a password): create a PocketBase API key / admin token and paste it here.
POCKETBASE_ADMIN_TOKEN=your_admin_token_or_api_key
# Fallback (if you don't have a token):
POCKETBASE_ADMIN_EMAIL=admin@example.com
POCKETBASE_ADMIN_PASSWORD=your_admin_password

# Optional: super-admin user IDs for the Mutual Credit bot (bypass Telegram group admin checks)
# SUPER_ADMIN_USER_IDS=[123456789]

# Credit System Defaults
CREDIT_BASE_LIMIT=100
CREDIT_PER_DEAL=50

# Website Configuration (for review links and responses)
# The public URL of your trust.bigislandbulletin.com website (no trailing slash)
COMMONTRUST_WEB_URL=https://trust.bigislandbulletin.com
# Optional: public image URL sent to new users on /start
# COMMONTRUST_HOWTO_IMAGE_URL=https://trust.bigislandbulletin.com/how-to-image.png

# Review Response Secret (shared between bot and web app)
# Generate with: openssl rand -hex 32
REVIEW_RESPONSE_SECRET=your_secret_here_generate_with_openssl_rand_hex_32

# AI Report Analysis (Venice.ai — for scam report triage)
VENICE_API_KEY=your_venice_api_key_here
# Model for report analysis (default: llama-3.3-70b)
# Options: llama-3.3-70b, deepseek-ai-DeepSeek-R1, qwen3-235b-a22b-thinking-2507, etc.
AI_MODEL=llama-3.3-70b
